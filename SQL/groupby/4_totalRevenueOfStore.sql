/*4. Find the total revenue generated for each month.
5. Determine the total revenue generated by each store.*/

use sakila
select store.store_id, sum(amount) from store
 join staff on
store.address_id= staff.address_id join payment on
staff.staff_id=payment.staff_id
group by amount

select store.store_id,sum(amount) from store 
join staff on store.store_id=staff.store_id join 
payment on staff.staff_id=payment.staff_id group by amount


